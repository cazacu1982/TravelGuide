/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { AppPipe } from './app.pipe';

describe('AppPipe', () => {
  it('create an instance', () => {
    let pipe = new AppPipe();
    expect(pipe).toBeTruthy();
  });
});
